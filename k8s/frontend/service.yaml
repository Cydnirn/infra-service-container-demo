# Frontend Service — LoadBalancer (external browser access).
# After deploying, find the external URL with:
#   kubectl get svc student-frontend -n student-management
apiVersion: v1
kind: Service
metadata:
  name: student-frontend
spec:
  type: ClusterIP
  selector:
    app: student-frontend
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
