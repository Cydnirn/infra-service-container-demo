# Frontend Service — ClusterIP (internal only).
# All external traffic goes through the Ingress (ALB).
apiVersion: v1
kind: Service
metadata:
  name: student-frontend
spec:
  type: ClusterIP
  selector:
    app: student-frontend
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
